Create an exportable configuration file so that users can customize the installed packages.

Allow users to set the root URL for all packages and provide the option to specify the OPENAI_API_KEY.

This configuration should be accessible in the main project or through a centralized config file.