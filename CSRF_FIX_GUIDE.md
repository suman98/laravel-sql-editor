# CSRF Token Mismatch Fix\n\n## Issue\n\n**Error**: \"CSRF token mismatch\" when testing database connections via POST /test-connection\n\n## Root Causes & Solutions\n\n### ✅ Fixes Applied\n\n1. **Safer CSRF Token Retrieval**\n   - **Before**: `document.querySelector('meta[name=\"csrf-token\"]').content`\n   - **After**: `document.querySelector('meta[name=\"csrf-token\"]')?.content || ...`\n   - Added optional chaining and fallback sources\n   - Now also checks for hidden input fields with name=\"_token\"\n\n2. **Added `credentials: 'same-origin'`**\n   - Ensures browser sends session cookies with the request\n   - Laravel CSRF verification relies on session cookies\n   - **Important**: Without this, the session won't be sent\n\n3. **Removed Duplicate Token in Body**\n   - Only sends token via `X-CSRF-TOKEN` header\n   - Prevents conflicts between header and body tokens\n\n4. **Improved Error Handling**\n   - Added 419 status check (session expired)\n   - Better error messages showing exact issue\n   - Console logging for debugging\n\n## Files Changed\n\n### `/resources/views/editor.blade.php`\n\n**Change 1: Token Retrieval (Line ~2591)**\n```javascript\n// BEFORE\nconst csrfToken = document.querySelector('meta[name=\"csrf-token\"]').content;\n\n// AFTER\nconst csrfToken = document.querySelector('meta[name=\"csrf-token\"]')?.content || \n                 document.querySelector('input[name=\"_token\"]')?.value || \n                 document.querySelector('[name=\"_token\"]')?.content || '';\n```\n\n**Change 2: testConnection() Function (Line ~2790)**\n```javascript\n// Added checks and credentials\nif (!csrfToken) {\n    throw new Error('CSRF token not found. Please refresh the page.');\n}\n\nconst response = await fetch(url, {\n    method: 'POST',\n    headers: {\n        'Content-Type': 'application/json',\n        'Accept': 'application/json',\n        'X-CSRF-TOKEN': csrfToken  // Only in header\n    },\n    credentials: 'same-origin',     // Send cookies\n    body: JSON.stringify(conn)      // Don't include _token\n});\n```\n\n**Change 3: execute() Calls**\n- Added `credentials: 'same-origin'` to both execute endpoints\n- Added 419 status handling for expired sessions\n- Improved error messages\n\n## How CSRF Protection Works in Laravel\n\n1. **Token Generation**\n   - Laravel generates a unique token for each session\n   - Token is stored in session\n   - Token is also placed in HTML (meta tag or hidden input)\n\n2. **Token Verification** (VerifyCsrfToken Middleware)\n   - When POST request comes in, middleware checks:\n     - `X-CSRF-TOKEN` header\n     - `_token` form field\n     - `X-XSRF-TOKEN` header\n   - Compares token with one in session\n   - If mismatch → 419 response\n\n3. **Session Linking**\n   - CSRF token is tied to session\n   - Session is transmitted via browser cookies (JSESSIONID/LARAVEL_SESSION)\n   - Without cookies (credentials), token can't be verified\n\n## Testing the Fix\n\n### Step 1: Verify Meta Tag\nOpen browser console (F12) and run:\n```javascript\n// Should show the CSRF token\ndocument.querySelector('meta[name=\"csrf-token\"]').content\n```\n\n### Step 2: Verify csrfToken Variable\n```javascript\n// Should NOT be empty\nconsole.log('CSRF Token:', csrfToken);\n```\n\n### Step 3: Test Connection\n1. Click \"Manage Connections\"\n2. Create a test MySQL connection to localhost\n3. Click \"Test\" button\n4. Check browser Network tab (F12 → Network)\n5. Look for `/test-connection` request\n6. Should see:\n   - Status: 200 or 422 (OK or validation error)\n   - NOT 419 (CSRF token mismatch)\n   - Header: `X-CSRF-TOKEN: [token]`\n   - Cookie: `XSRF-TOKEN=...` or `LARAVEL_SESSION=...`\n\n### Step 4: Check Response\n- **Success**: `{\"message\": \"Connection successful\", ...}`\n- **CSRF Error**: `{\"message\": \"CSRF token mismatch\"}` (419)\n- **Validation Error**: `{\"error\": \"Database name required\"}` (422)\n\n## If CSRF Error Still Occurs\n\n### Issue 1: Meta Tag Not Present\n**Symptom**: csrfToken variable is empty\n\n**Fix**: Ensure meta tag is in `editor.blade.php` head:\n```blade\n<meta name=\"csrf-token\" content=\"{{ csrf_token() }}\">\n```\n\n### Issue 2: Session Not Maintained\n**Symptom**: Request sent but session not found\n\n**Fix**: Check:\n1. Cookies are enabled in browser\n2. SameSite cookie setting in `.env`:\n   ```\n   SESSION_COOKIE_SAMESITE=Lax\n   ```\n3. Session middleware is registered in `config/app.php`\n\n### Issue 3: Wrong Route or Method\n**Symptom**: 404 or 405 errors\n\n**Fix**: Verify route:\n```bash\n# In terminal\nphp artisan route:list | grep test-connection\n```\n\nShould show:\n```\nPOST /sql-analyzer/test-connection\n```\n\n### Issue 4: Middleware Misconfiguration\n**Symptom**: CSRF always fails\n\n**Fix**: Check `routes/web.php`:\n```php\nRoute::group([\n    'middleware' => config('sql-analyzer.middleware', ['web']),\n], function () {\n    // Routes here use 'web' middleware by default\n    // 'web' includes VerifyCsrfToken\n});\n```\n\n## Advanced Debugging\n\n### Enable Debug Logging\n\n**In `config/logging.php`**:\n```php\n'channels' => [\n    'debug' => [\n        'driver' => 'single',\n        'path' => storage_path('logs/debug.log'),\n        'level' => 'debug',\n    ],\n],\n```\n\n**In controller**:\n```php\nuse Illuminate\\Support\\Facades\\Log;\n\nLog::debug('CSRF Token', [\n    'token_from_header' => $request->header('X-CSRF-TOKEN'),\n    'token_from_session' => $request->session()->token(),\n    'match' => hash_equals($request->token(), $request->header('X-CSRF-TOKEN')),\n]);\n```\n\n### Check Network Request\n\nIn browser DevTools (F12 → Network):\n1. Filter for `/test-connection`\n2. Click the request\n3. **Headers tab**:\n   - Should see `X-CSRF-TOKEN: [value]`\n   - Should see `Cookie: LARAVEL_SESSION=...`\n4. **Request Payload**:\n   - Should show connection JSON (NOT with `_token` parameter)\n5. **Response tab**:\n   - If 419: CSRF issue\n   - If 422: Validation issue (OK, means CSRF passed)\n   - If 200: Success\n\n## Configuration\n\n### `.env` Settings\n\n```env\n# Session\nSESSION_DRIVER=cookie\nSESSION_LIFETIME=120\nSESSION_ENCRYPT=false\n\n# CSRF\nCRYPT_CIPHER=AES-256-CBC\n\n# Cookie SameSite\nSESSION_COOKIE_SAMESITE=Lax\nSESSION_COOKIE_SECURE=true  # Use only with HTTPS\nSESSION_COOKIE_HTTP_ONLY=true\n```\n\n### `config/session.php`\n\n```php\nreturn [\n    'driver' => env('SESSION_DRIVER', 'database'),\n    'lifetime' => env('SESSION_LIFETIME', 120),\n    'expire_on_close' => false,\n    'encrypt' => env('SESSION_ENCRYPT', false),\n    'cookie' => env('SESSION_COOKIE', 'LARAVEL_SESSION'),\n    'path' => '/',\n    'domain' => env('SESSION_DOMAIN', null),\n    'secure' => env('SESSION_SECURE_COOKIES', false),\n    'http_only' => true,\n    'same_site' => env('SESSION_SAME_SITE', 'Lax'),\n];\n```\n\n## Changes Made This Session\n\n| File | Change | Lines | Status |\n|------|--------|-------|--------|\n| editor.blade.php | Token retrieval with fallbacks | 2591 | ✅ |\n| editor.blade.php | testConnection() improvements | 2790 | ✅ |\n| editor.blade.php | execute() credentials added | 3647, 3908 | ✅ |\n| editor.blade.php | 419 error handling | Multiple | ✅ |\n\n## Test Scenarios\n\n### Scenario 1: Valid Connection (MySQL)\n1. Connection: `host=localhost, port=3306, db=mysql, user=root, pass=''`\n2. Expected: Status 200, Success message\n3. Actual: ?\n\n### Scenario 2: Invalid Host\n1. Connection: `host=nonexistent.local`\n2. Expected: Status 200/422, Error about host not found\n3. Actual: ?\n\n### Scenario 3: After Page Reload\n1. Reload page (Cmd/Ctrl+R)\n2. Try test connection again\n3. Expected: Should work (new CSRF token)\n4. Actual: ?\n\n### Scenario 4: Browser Extended Sleep\n1. Leave page open for 2+ hours\n2. Try test connection\n3. Expected: 419 \"Session expired\"\n4. Actual: ?\n\n## Next Steps if Still Broken\n\n1. **Check server logs**: `storage/logs/laravel.log`\n2. **Check browser console**: F12 → Console (any errors?)\n3. **Check Network tab**: F12 → Network → XHR (what status?)\n4. **Clear session**: `php artisan session:clear`\n5. **Clear cache**: `php artisan cache:clear`\n6. **Re-test**: Refresh page and try again\n\n## Documentation\n\n- [Laravel CSRF Protection](https://laravel.com/docs/guard#csrf-protection)\n- [VerifyCsrfToken Middleware](https://github.com/laravel/framework/blob/master/src/Illuminate/Foundation/Http/Middleware/VerifyCsrfToken.php)\n- [MDN: Fetch API and CORS](https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API)\n"}}
